# 初級(Beginner)ストーリーベース再設計案 — ①topicリスト & ②物語割り当て(ドラフト)

`docs/curriculum.md`(単元・カテゴリベースの旧目次)を old とし、初級を「複数の自然な物語」で再構成するための作業ドキュメント。既存の`ip-address-wifi`3チャプター(Pipのチーズ写真の物語)も含め、ゼロベースで見直した結果を反映している。

進め方は3ステップ:
- **①** 初級に含めるべき学習項目を、カテゴリ分けせず粒度細かく洗い出す(下記)
- **②** それらを自然に教えられる物語をいくつか設計し、topicを割り振る(下記)
- **③** 完了基準(網羅率95%以上・各物語がスムーズに繋がる・易→難の段階学習になっている)を満たすか検証する → 満たさなければ①からではなく②の割り振りをやり直す

---

## ① 初級 学習項目リスト(粒度細かく、カテゴリ分けなし)

出典: `docs/curriculum.md`の初級1-1〜1-4を分解したもの(大半)+ `docs/original-plan.md`にのみ書かれている具体的パラメータ + 既存docsに載っていないが初級相応と判断し追加したもの(★印)。全105項目。

1. ネットワークとは何か(機器同士がデータをやり取りする仕組み)★
2. LAN(1つの場所に閉じた、1台の共有ルーターを介するネットワーク)とは何か
3. LANと「インターネット」の違い(インターネット=世界中の他の全ネットワークが繋がったもの)
4. ルーターの役割(LANの唯一の出入り口、通信を転送する)
5. ルーターとモデムの違い★
6. 有線接続(ケーブル)と無線接続(電波)の違い
7. Wi-Fiとは何か(電波でLANに参加する仕組み)
8. Wi-Fiの周波数帯(2.4GHz/5GHz)とそのトレードオフ(到達距離 vs 速度)★
9. Wi-Fi電波を妨げる物理的障害(壁、電子レンジ等のノイズ源)★
10. Wi-Fiのパスワード/暗号化が守っているもの(概念レベル)★
11. IPアドレスとは何か(ネットワークが機器に割り当てる識別番号)
12. IPアドレスの比喩(街の住所)
13. IPアドレスの書式(ドット区切りの4つの数字)
14. 1つの数字が0〜255までな理由(1バイト=2^8=256通り)
15. IPv4全体のアドレス数(2^32 ≈ 43億)
16. IPv4アドレス枯渇問題
17. IPv6が存在する理由(枯渇への対策、1文で触れる程度)
18. MACアドレスとは何か(工場出荷時に焼き付けられた固有ID)
19. MACアドレスとIPアドレスの対比(固定 vs 割り当て)
20. 機器が通信するには一意なアドレスが必要であること
21. IPアドレスが重複するとどうなるか(両方が応答してしまい送信元が区別できない)
22. DHCPとは何か(アドレスを自動的に割り当てる仕組み)
23. DORA(Discover→Offer→Request→Ack)という手順の名称(DHCPそのものの名前ではないという区別)
24. DHCPが通常IP重複を防いでいる理由(ルーターがリース済みアドレスの表を持つ)
25. 手動固定IP設定がDHCPの保護を回避してしまうこと
26. DHCPの「リース」という考え方(永続的な割り当てではない)
27. デフォルトゲートウェイとは何か(宛先がLAN外の時に頼る、唯一知っているアドレス)
28. 「同じサブネットか」の判定が直接会話かゲートウェイ経由かを決めること
29. サブネットとは何か・なぜネットワークを分割するのか
30. サブネット分割の動機1: ブロードキャストが届く範囲を絞る
31. サブネット分割の動機2: 組織の部署ごとにアドレス管理を委譲できる
32. サブネット分割の動機3: 目的別にデバイスを論理的にグルーピングできる
33. サブネットマスク/CIDR表記(/24等)の意味
34. ドット付き10進表記のマスク(255.255.255.0)との対応
35. 32ビット中どこまでがネットワーク部かという考え方
36. ネットワークアドレス(サブネット内最初のアドレス、サブネット自体を指す、割り当て不可)
37. ブロードキャストアドレス(サブネット内最後のアドレス、全員に届く、割り当て不可)
38. ホストアドレス(それ以外、実際に割り当て可能)
39. NアドレスのブロックにホストがN-2台入る、という計算
40. ネットワークビットを1つ増減すると、ブロックサイズが倍/半分になること(2^(32-prefix))
41. ARPとは何か(「このIPは誰のものか」のブロードキャスト問い合わせと応答)
42. ARPが必要な理由(LAN内配送はMACアドレスが要る、IPだけでは届かない)
43. ブロードキャストの仕組み(1つのメッセージが同じサブネット内全員に届く)
44. ルーター内部のL2スイッチの役割(宛先によってNAT行きか直接配送かを振り分ける)
45. MACアドレステーブルによる学習・転送という仕組みが存在すること(深入りしない、中級2-2で扱うと明記するだけ)
46. パケットがWi-Fi電波(物理層)に乗ってルーターまで届くこと
47. CGNAT(Carrier-Grade NAT)とは何か(ISPが複数家庭を自社の巨大プライベート網に収容する)
48. CGNAT専用に予約された実在の範囲100.64.0.0/10(RFC 6598)
49. パケットが複数階層のアドレスを同時に持つこと(自宅内プライベートIP+ISP発行プライベートIP)
50. NAT/NAPTの変換メカニズム(プライベートIP:ポート ⇔ パブリックIP:ポートの対応表)
51. NAPTでポート番号が必要な理由(1つのパブリックIPを複数機器/複数家庭で共有するため)
52. NAT変換テーブルが無いと応答が戻れないこと(戻りの応答は対応表を逆にたどる)
53. プライベートIPアドレス(RFC1918、自分のLAN内でのみ意味を持つ、世界中で再利用可能)
54. グローバル(パブリック)IPアドレス(世界で一意)
55. プライベート/グローバルの2種類が存在する理由(グローバルアドレスだけでは全世界の機器を賄えない)
56. インターネット上のダイナミックルーティング(経路は動的に選ばれる、という存在だけ触れる。詳細な仕組み=コストによる経路選択・障害時の再収束は中級2-3「ルーティング」に譲ると明記)
57. パケットが小さな単位(パケット)に分割されて送信されること
58. 分割されたパケットが宛先で1つのデータに再構成されること
59. サーバーとは何か(リクエストを受け取り応答する側の機器)★
60. クライアントとサーバーという役割分担★
61. データを離れた場所へ「アップロードする」という基本概念★
62. 「クラウドサーバー」という一般語彙(遠く離れた場所にあるサーバー)★
63. DNSとは何か(ドメイン名とIPアドレスを結びつける、分散型・階層型の仕組み)
64. URLの構造(スキーム/ホスト(ドメイン名)/ポート/パス/クエリ)
65. DNSが解決するのはURLの「ホスト」部分だけであること
66. ドメイン名の階層構造(ルート(.)→TLD(.com)→セカンドレベル(example)→サブドメイン(www))
67. ドメイン階層は右から左へ権限が委譲されていくこと
68. なぜIPを直接使わずドメイン名を使うか(サーバー移転や負荷分散でIPは変わりうる)
69. 名前解決の実アーキテクチャ: スタブリゾルバ(端末内)
70. 再帰リゾルバ(プロバイダやパブリックDNS、例:8.8.8.8/1.1.1.1)
71. ルート→TLD→権威DNSサーバーという階層をリゾルバが辿ること
72. 再帰問い合わせ(クライアントがリゾルバに「丸投げ」する)という方式
73. 反復問い合わせ(リゾルバ自身がルートから一段ずつ辿る)という方式
74. キャッシュは複数層あるという単純化の明示(ブラウザ・OSにも独自の短命キャッシュがある)
75. キャッシュの目的(毎回全チェーンを辿るコストを避ける)
76. TTL(生存時間)がキャッシュの有効期限を決めること
77. TTL切れ後は再度権威DNSに問い合わせに行くこと(キャッシュミス)
78. TTLは速度と鮮度のトレードオフであること
79. レジストラ(ドメインを買う場所)とDNSホスティング(実際に答える場所)は別物であること
80. NSレコード(TLDから実際のDNSホストへの委任情報)
81. Aレコード(ドメイン名→IPv4アドレス)
82. AAAAレコード(ドメイン名→IPv6アドレス)
83. CNAMEレコード(別名から別のドメイン名への解決、名前解決がもう一段挟まる)
84. MXレコード(メールの配送先)
85. TXTレコード(任意のテキスト、ドメイン所有権証明やSPF/DKIM)
86. SOAレコード(ゾーンの管理情報・タイマー)
87. hostsファイル(どこにも問い合わせる前に見る、ローカルの手動上書き一覧)
88. 端末が再帰リゾルバのアドレスをどう知るか(DHCPがIPアドレスと同時に配布)
89. 名前解決の一気通貫フロー(URL→hostsファイル→ローカルキャッシュ→Root→TLD→権威→Aレコード→キャッシュ→端末へ返却)
90. NXDOMAIN(そのような名前は存在しないという確定的な回答)
91. DNS解決後はブラウザ自身がTCP/TLS/HTTPで接続を引き継ぐという役割の切れ目
92. リクエスト/レスポンスモデル
93. HTTPはステートレス(1回ごとに完結)であること
94. GET(取得)の意味
95. POST(送信・作成)の意味
96. 200 OK / 404 Not Foundという代表的なステータスコード★
97. ステータスコードの分類(2xx成功/3xxリダイレクト/4xxクライアント側/5xxサーバー側)
98. 502 Bad Gateway(上流から不正な応答が返ってきた)の意味
99. 504 Gateway Timeout(上流から応答自体が来ない)の意味
100. どのコンポーネント(LB/FW/Webサーバー/DB)が原因かを切り分ける診断的な考え方
101. ファイアウォールの基本役割(境界で通信の許可/遮断を決める門番)
102. ステートフルインスペクション(自分から出た通信への戻りは自動的に許可される)
103. ルールは上から順に評価され、最初にマッチしたルールが適用されること
104. デフォルト拒否(明示的に許可したものだけ通す)とデフォルト許可という設計思想の違い
105. IN/OUT(通信の方向)とIPアドレス・ポート番号の組み合わせでルールが構成されること

**意図的に含めなかった/中級以上に残すべきと判断した候補**(参考として明記):
- ポート番号そのものの詳しい仕組み(2-1、エフェメラルポート等) — HTTP/Firewallの中で「ポート番号」という語自体は出てくるが、仕組みの詳細教育は中級のまま(下記Chapter 8で「存在する」ことだけ軽く触れる)
- ポートフォワーディング/外部からの接続受付(2-5) — CGNAT/NAT/NAPTの「送信方向」は初級で扱うが、「受信方向」は中級のまま
- ダイナミックルーティングのコスト計算・収束時間(2-3) — 存在だけ触れ、詳細は中級
- MACアドレステーブルの学習・転送の仕組み自体(2-2) — 存在だけ触れ、詳細は中級
- 帯域幅(bandwidth) vs 遅延(latency)という対比 — **却下。組み込まない**(自然な物語が弱いため)

---

## ② 物語への割り当て(確定版・v2)

**設計方針:** 既存の「ネズミくんPip」の物語を土台として維持しつつ、DNS・HTTP・Firewallを**Pipの物語の中に時系列的に自然な形で組み込む**。

**Firewallの独立/統合の判断基準(ご指示通り):** 時系列上、既存の別チャプターの**間に**独立したイベントとして自然に置けるものは独立チャプターにする。既存シーンの**最中に**しか成立しない(=同時に起きている出来事)ものは、そのシーンに統合する。この基準で firewall の5項目を2つに分割した:
- **ステートフルインスペクション(項目102)** — 「Chapter 3で自分が送った通信への返信が、なぜ関所を通って戻ってこられるのか」という、Chapter 3の返信シーンと**全く同じ瞬間**の出来事。分離できないので Chapter 3 に統合。
- **ルール評価順・デフォルト拒否・IN/OUT+ポートの組み合わせ(項目101, 103–105)** — こちらは「見知らぬ誰かが外から接続を試みる」という、**Chapter 3の送信イベントとは別の日・別の出来事**として成立する。独立した新チャプターにできる。

### Chapter 1: "Meet the Network"(新規、易) — 項目 1–20, 59–62

**単なる語彙紹介にしないための筋書き:** 「Pipの部屋だけWi-Fiが弱い」という小さな謎から始める。
1. Pipが自分の部屋でWi-Fiが遅いことに気づく(問題提起)
2. 家の中を歩き回って電波の強さを確かめる → ルーターから離れる・壁を挟むと弱くなる(障害物)、2.4GHz/5GHzの切り替えで距離と速度が変わる(周波数帯)ことを発見
3. リビングにある機器一覧を数え直す過程で「これがルーター、その後ろの箱がモデム」と気づく(ルーター/モデムの違い)、「自分のスマホもお父さんのPCも、このルーター経由でしか外と話せない」と気づく(LAN)
4. でも「さっき見た遠くのニュースサイトにも繋がった」ことを思い出し、「じゃあ家の外には何があるんだろう」と疑問に思う(LAN vs インターネット)
5. Wi-Fiにパスワードが要る理由を親に聞く(パスワード/暗号化)
6. スマホの設定画面を開き、自分のスマホに数字が2つ表示されているのに気づく(IPアドレスとMACアドレスの対比、書式、算数、重複したら困ること)
7. 「さて、あのチーズ好き掲示板(サーバー)に写真をアップロードしよう」で締め、Chapter 2への橋渡し(サーバー/クライアント/クラウドの語彙はここで自然に出す)

問題提起→調査→理解→次の行動、という起承転結を持たせることで、語彙の羅列にならないようにする。

### Chapter 2: "Finding the Board"(新規、易〜中、**分割せず1本で執筆**) — 項目 63–91

Pipが「cheeselovers.com」と打つところから物語開始。DNSの全項目(URL構造、階層、再帰/反復、キャッシュ/TTL、レコード種別、レジストラ/ホスティングの違い、hostsファイル)をPip視点の一気通貫の物語として新規に書く。最後に「203.0.113.50」という数字を手に入れて終わり、Chapter 3に地続きで繋がる。まずは分割せずに書いてみて、ステップ間の繋がりが悪ければ後で前後半に分ける。

### Chapter 3: "Leaving the House"(既存を拡張、中) — 項目 21, 27, 44, 46–58, 102

現行Chapter 1後半(NAT/CGNAT/プライベート・グローバルIP)をそのまま維持しつつ、既存の「国境の関門(checkpoint)」シーンの返信の場面に、**ステートフルインスペンション**の一言を追加する(「これは君が自分から出した通信への返事だから、通していいんだ」)。

### Chapter 4: "Who Goes There?"(新規、中、独立) — 項目 101, 103–105

Chapter 3の少し後、別の出来事として新設。見知らぬ相手が外からPipの家(お父さんのPCなど)に接続を試みる場面。お父さんがルーターのルール一覧を上から順に確認し、マッチする最初のルールが適用される様子、「明示的に許可したものだけ通す」という基本方針、IN/OUT+IPアドレス+ポート番号の組み合わせでルールが構成されることを描く。

### Chapter 5: "The Short Way Home"(既存を維持、中〜難) — 項目 28–43

現行Chapter 2そのまま(CIDR/サブネットマスク/ARP)。既存の作り込みを尊重し、大きな変更はしない。

### Chapter 6: "Momo Gets Online"(既存を維持、中) — 項目 22–26

現行Chapter 3そのまま(DHCP/DORA/IP重複)。

### Chapter 7: "Did It Work?"(新規、易、締めくくり) — 項目 92–100

Chapter 3で「投稿された」ところまで描いたPipの物語に対し、「本当に届いたか確認する」エピローグとして新設。GET/POST、ステータスコードの分類、502/504、診断的な考え方を、Pipが掲示板を確認する/一度失敗してリトライする、という短い物語で扱う。

### Chapter 8: "Seeing It All at Once"(新規、易、総復習)

`concept.md`の方針(具体→抽象、階層モデルは終盤の総復習として置く)に沿って追加。Pipがこれまでの冒険を振り返り、Wi-Fi電波(物理層)→MACアドレス(データリンク層)→IPアドレス/ルーティング(ネットワーク層)→(ポート番号が存在することだけ軽く触れる、詳細は中級)→DNS/HTTP(アプリケーション層)という積み重ねとして捉え直す。新しい技術内容は増やさず、既存イラストを再利用した「振り返り」に徹する。

---

## 網羅率(v2)

105項目中、上記8チャプターで**105項目中100項目**をカバー(約95%)。未割り当ては「意図的に含めなかった候補」の中級行き4項目のみ(帯域幅/遅延は却下により削除)。

## 決定事項まとめ(ご回答を反映)

1. Firewall → ステートフル部分はChapter 3に統合、ルール評価・デフォルト拒否部分は独立Chapter 4として新設
2. Chapter 2(DNS)→ 分割せず1本で執筆してみる
3. OSI/TCP-IP階層モデルの総復習章 → Chapter 8として追加
4. 帯域幅 vs 遅延 → 却下、組み込まない
5. Chapter 1 → 「Wi-Fiが弱い」という謎解き型の筋書きを追加し、単なる語彙紹介にならないようにした

次は実装(コード化)に進んでよいか、あるいはこのv2案自体にさらに調整が必要か、ご確認をお願いします。

---

## ③ Unit分割・章順序の見直し(v3、2026-08-31時点・暫定決定)

実装を進める中で、旧`ip-address-wifi` Unit(Chapter 1 Leaving the House / Chapter 2 Short Way Home / Chapter 3 Momo Gets Online)は**削除予定**であることが判明した。この3チャプターはUnit名を`[old] IP Address & Wi-Fi Connection [to be deleted]`に変更した上でそのまま残し、代わりに**新しいUnit「DRAFT: Pip's Network Story」**(`/beginner/pips-network-story`、実装は`features/beginner/ip-address-wifi/DraftBeginnerPlayground.tsx`)を切り出して、そちらでPipの一続きの物語として再構築していく。

### 章順序(v4、暫定)

1. **Meet the Network**(既存どおり)
2. **The Short Way Home**(旧v2案では5番目だったが、**Finding the Boardより前**に繰り上げ — 理由は下記。さらに、Momo Gets Onlineより先に据えることで、Meet the Networkの「アップロードしよう」という本筋がすぐ続く形にした)
3. **Momo Gets Online**(DHCP — ストーリー・主人公は全面的に描き直してよく、重要なのは項目22-26がわかりやすく学習できることのみ、という前提で再設計 — 詳細は下記)
4. **Finding the Board**(既存どおり、DNS)
5. **Leaving the House**(既存を拡張、ただし**エンディングを変更** — 下記)
6. **Who Goes There?**(既存どおり4番目だったが、依存関係上ここが正しい位置であることを確認 — 下記)
7. (未定 — インターネットに出たパケットが実際にcheeseloversのサーバーに届くかどうかを解決する章。中級〜上級にまたがるため単一の章では完結しない。着地は保留中)

Did It Work? / Seeing It All at Once の位置づけは②から変更なし(今回の見直し対象外)。

**v3からの変更点:** Momo Gets OnlineとThe Short Way Homeの順序を入れ替え、Short Way HomeをChapter 2、Momo Gets OnlineをChapter 3にした(ユーザーの明示的な指示)。

### 変更理由

- **Short Way HomeをMeet the Networkの直後(Chapter 2)に据えた理由:** Meet the Networkは「さあアップロードしよう」で終わり、Short Way Homeの冒頭(お父さんが「パパにも送ってくれよ」と頼む場面)はまさにその直後に起きる出来事として書かれている。Momo Gets Onlineはチーズ写真の本筋と無関係な横道なので、本筋の物語がすぐ続くよう、横道より先に本筋の一幕(Short Way Home)を置く。
- **Momo Gets OnlineをChapter 3に据えた理由・再設計方針:** DHCP(項目22-26)は「そもそもどうやってネットワークに参加し、アドレスをもらうか」という、ARP/サブネット(Short Way Home)やDNS(Finding the Board)よりもさらに手前・より基礎的な仕組みではあるが、Short Way Homeの直後という位置でも(Meet the Networkの直後という位置と同様に)まだ十分に鮮度が高いタイミングで扱える。物語・主人公は旧来の「Momo(電球)視点」から作り直し、**Pipが観察者として絡む形**にする — 具体的には、`network.ts`の`DEFAULT_DEVICES`に元々含まれ、Meet the NetworkやShort Way HomeのLanMap上にずっと背景として映り込んでいる`iot`(スマート電球)を正式に「Momo」と命名し、「お父さんが新しい電球(Momo)を廊下に取り付け、Pipが『誰も番号を入力していないのに、もう繋がっている』と気づいてDORAの仕組みを目撃する」という筋にする。IPアドレス重複を回避する場面では、衝突しかけの相手をPip自身の192.168.1.10にすると、既読者にとって具体的な対比になる。
- **Short Way HomeをFinding the Boardより前にした理由:** (a) 依存関係が非対称。DNS(Finding the Board)は「すでにIP接続ができている」ことが前提の応用的な仕組みだが、LAN内配送(Short Way Home)の仕組みはDNSに一切依存しない、より下位層の基礎知識。(b) 難易度。実際にチャットの中で技術的に洗い出した結果、「インターネットに出てからcheeseloversのサーバーに着くまで」に発生する概念は22項目中わずか2項目しか初級ではなかった(ほとんどが中級・上級、または`curriculum.md`未収録)のに対し、「PipがLAN内でお父さんのPCに写真を共有する」に発生する概念は18項目中14項目が初級だった。易しい方を先に置く方が段階学習として自然。
- **Leaving the Houseのエンディングを変更した理由:** 上記の技術的洗い出しの結果、「サーバーに実際に届く」ところまで初級のまま描くのは無理があると判明したため、**パケットがインターネットに出たところで意図的にクリフハンガーにする**方針に変更(「長い道のりを経て、チーズの写真はやっとinternetという広い空へ出ることができました。果たしてcheeseloversへ投稿できるのでしょうか？…続きは後の章へ」)。これに伴い、以前この章に実装していた再構成(item 58の2回目)・返信の往路(NAT逆変換・ステートフルインスペンション item 102)は一旦削除した。**item 102(ステートフルインスペクション)の置き場所は現時点で未定**— 元々はこの章の返信シーンに統合する予定だったが、返信シーン自体が(サーバーに届くかどうか未解決なので)描けなくなったため、Who Goes There?章に統合するか、後日の継続章に回すか、要検討。
- **Who Goes There?をLeaving the Houseの直後にした理由:** ファイアウォールのIN/OUT+IPアドレス+ポート番号の組み合わせでルールを説明する章のため、ポート番号/NATの概念(Leaving the Houseで初めて登場)が既習である必要がある。Short Way HomeとFinding the Boardの間に置く案も検討したが、その時点ではポートの概念がまだ無いため不適。

この構成は**あくまで仮決め**。実装(コード化)は上記の順序で進めるが、Who Goes There?以降の詳細な章立て・item 102の着地点は今後さらに検討する。
