/** Single source of truth for the app's display name. Change it here and
 * it propagates everywhere: the browser tab title (via layout.tsx's
 * metadata template, so every page's title becomes "<Page> — {APP_NAME}"
 * automatically), the nav bar brand link, and the breadcrumb root. */
export const APP_NAME = "Network Interactive Lab";
